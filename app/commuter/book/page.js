import { CalendarDays } from 'lucide-react';
import PlaceholderPage from '@/components/layout/PlaceholderPage';

/** Commuter booking placeholder. */
export default function BookPage() { return <PlaceholderPage title="Book a ride" description="Choose a shuttle route and reserve a seat for your next trip." icon={CalendarDays} />; }
