import { ClipboardList } from 'lucide-react';
import PlaceholderPage from '@/components/layout/PlaceholderPage';

/** Commuter trips placeholder. */
export default function TripsPage() { return <PlaceholderPage title="My trips" description="See your upcoming and previous shuttle bookings in one place." icon={ClipboardList} />; }
