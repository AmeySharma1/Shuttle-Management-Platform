import { CarFront } from 'lucide-react';
import PlaceholderPage from '@/components/layout/PlaceholderPage';

/** Driver day placeholder. */
export default function DriverPage() { return <PlaceholderPage title="My day" description="See your duty, assigned route and trips for today." icon={CarFront} />; }
