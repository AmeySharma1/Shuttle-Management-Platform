import { LayoutDashboard } from 'lucide-react';
import PlaceholderPage from '@/components/layout/PlaceholderPage';

/** Admin overview placeholder. */
export default function AdminPage() { return <PlaceholderPage title="Overview" description="See a clear summary of shuttle operations and today's activity." icon={LayoutDashboard} />; }
