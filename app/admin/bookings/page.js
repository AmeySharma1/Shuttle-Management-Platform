import { ClipboardList } from 'lucide-react';
import PlaceholderPage from '@/components/layout/PlaceholderPage';

/** Admin bookings placeholder. */
export default function BookingsPage() { return <PlaceholderPage title="Bookings" description="Review and manage commuter ride requests here." icon={ClipboardList} />; }
