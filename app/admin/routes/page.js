import { MapPinned } from 'lucide-react';
import PlaceholderPage from '@/components/layout/PlaceholderPage';

/** Admin routes placeholder. */
export default function RoutesPage() { return <PlaceholderPage title="Routes" description="Review shuttle routes, stops and schedules here." icon={MapPinned} />; }
