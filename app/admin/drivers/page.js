import { Users } from 'lucide-react';
import PlaceholderPage from '@/components/layout/PlaceholderPage';

/** Admin drivers placeholder. */
export default function DriversPage() { return <PlaceholderPage title="Drivers" description="Keep track of driver assignments and availability here." icon={Users} />; }
